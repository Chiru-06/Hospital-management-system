import React from 'react';
import { Typography } from '@mui/material';

const Billing: React.FC = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Billing
      </Typography>
      {/* Add billing management functionality here */}
    </div>
  );
};

export default Billing; 